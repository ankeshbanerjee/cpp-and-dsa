#include <bits/stdc++.h>
using namespace std;

int main (){
    // to store the weights of the edges,
    // in adjacency matrix, mark adj[u][v] = weight instead of 1
    // in adjacency list, create pair to store the neighbours and the weight
    // i.e., pair <vector<int>, int> adj[n+1];
return 0;
}