#include <iostream>
using namespace std;

int main (){

    // // size

    // int temp[10] = {1, 2, 3};
    // cout << sizeof(temp) << endl;

    // int *ptr = &temp[0];
    // cout << sizeof (ptr) << endl;




    // // address

    // int a[20] = {1,2,3};
    // cout << &a[0] << endl;

    // int * p = &a[0];
    // cout << &p << endl; // both are different




    // symbol table

    // int arr[10];
    
    // //ERROR
    // // arr = arr + 1; // this cannot be changed. 'arr' is mapped with the address of the first element of the array in symbol table

    // int * ptr = arr;
    // cout << ptr << endl;
    // ptr = ptr + 1;
    // cout << ptr << endl;

return 0;
}