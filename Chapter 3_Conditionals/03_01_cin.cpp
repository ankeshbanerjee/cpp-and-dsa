#include <iostream>
using namespace std;

int main (){
    int a, b;
    cin >> a >> b;
    // we can use 'space', 'enter' or 'tab' after entering the value of a and before enterring the value of b
    cout << a << endl;
    cout << b << endl;
}