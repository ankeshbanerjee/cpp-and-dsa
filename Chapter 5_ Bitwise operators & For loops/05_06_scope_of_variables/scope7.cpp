#include <iostream>
using namespace std;

int main (){
if (1){
    int b = 1;
    if (1){
        cout << b << endl; 
    }
}
}

// we can redefine a variable in the smaller blocks or,
// we can use the variable of the bigger block in smaller blocks because the variable of bigger block also exists in the smaller blocks under it. 